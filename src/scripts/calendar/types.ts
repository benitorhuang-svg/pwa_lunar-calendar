/**
 * Calendar Type Definitions
 * 定義日曆區域使用的所有介面與型別
 * Definitions used across the Calendar module
 *
 * Note: Core event types are now centralized in src/scripts/types.ts
 */

export type {
    DateSelectedDetail,
    NavigateMonthDetail,
    RenderCalendarDetail,
    ToggleGridViewDetail,
    UpdateCalendarTitleDetail,
} from "../types";
